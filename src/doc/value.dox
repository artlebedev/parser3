/**	@page Value Value

«начени€, которыми оперирует Parser, есть экземпл€ры классов, производных от Value.
Value знает свой тип [Value::type], который используетс€ дл€ выводов сообщений об ошибках.
“ип также можно проверить в
@verbatim
^if($variable is hash){когда в переменной hash}{когда не hash}
@endverbatim

Value может хранить элементы [Value::get_element, Value::put_element].
—амый простой случай - hash[VHash].


*/
