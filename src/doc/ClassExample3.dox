/**	@page ClassExample2  Пример создания класса (методы и поля)

Чтобы создать свой класс, состоящий не только из методов[таков, например, класс image], 
нужно создать простой класс(см. @ref ClassExample1), определяющий Methoded::used_directly в false.

После чего создать потомка Value, в котором переопределить Value::get_element и/или Value::put_element,
в конструкторе которого при вызове базового конструктора

@test 3

*/
