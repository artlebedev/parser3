/**	@page Compiler Compiler

Source code is compiled to internal form.
This happens with each request, result of compilation is not cached.
After compilation we have \b class, which got registered in Hash Request::classes().

@see compile.y, compile.C, compile_tools.h, compile_tools.C

*/
